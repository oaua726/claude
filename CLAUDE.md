# CLAUDE.md — 外部記憶(Obsidian)運用ルール

このリポジトリでセッションを始めたClaudeは、ローカルのObsidian Vaultを**外部記憶(外部脳)**として扱う。
チャットセッションを跨いでも文脈・決定事項・ナレッジ・過去のミスを維持すること。

接続方法とセットアップ手順は [docs/obsidian-setup.md](docs/obsidian-setup.md)。
業務そのものの安全ルール(メール送信は本人、等)は [docs/unyo-rule.md](docs/unyo-rule.md) が優先する。

---

## 0. Vault構成(フラット4カテゴリ)

```
Vault/
  Daily/       YYYY-MM-DD.md            その日の作業記録・引き継ぎ
  Decision/    YYYYMMDD_件名.md          判断・決定とその理由
  Knowledge/   テーマ名.md               解決した課題・手順・設定
  Mistakes/    YYYYMMDD_ミスの要約.md     ミスと正しい対処法
  _templates/  各カテゴリのテンプレート(カテゴリではない)
```

案件別のフォルダは作らない。案件の区別は**フロントマターの `project:` とタグ**で行う。
これで「◯◯様邸の記憶だけ辿る」も「全案件横断でナレッジを探す」も両方できる。

すべてのノートは先頭に以下のフロントマターを付ける:

```yaml
---
type: daily | decision | knowledge | mistake
date: YYYY-MM-DD
project: ◯◯様邸        # 案件に紐づかない場合は 全体
tags: [案件/◯◯様邸, 申請, 銀行]
---
```

---

## 1. 読み取りルール(セッション開始時・必須)

**指示の実行に入る前に、必ず以下の順でVaultを参照する。** 読まずに作業を始めない。

1. `Daily/` の**直近3日分**を読む → 前回の進捗・引き継ぎ事項を把握
2. これから触る案件がわかる場合、その案件のタグ/`project:` で**横断検索**し、
   `Decision/` と `Knowledge/` の関連ノートを読む → 既存の前提・決定済みルールを把握
3. `Mistakes/` を検索し、**同種の作業で過去にやらかしたミス**を確認する(→ 3節)

読んだ結果、前提や決定事項が今回の指示と食い違う場合は、勝手に上書きせず**先に指摘する**。

Vaultに該当記録がない場合は「過去記録なし」と明示してから作業に入る。
MCPが繋がっていない場合は**黙って進めず**、その旨を伝えてから(記録なしの前提で)作業する。

---

## 2. 書き込み・更新ルール(自動記録)

進捗や決定が発生したら、以下の分類に従って**自動でVaultに記録する**(毎回の許可取りは不要)。
ただし**追記のみ**。既存の記述の削除・上書きはしない(→ 4節)。

### Daily(作業記録)
- **いつ**: 作業が一区切りしたとき、およびセッション終了時
- **どこ**: `Daily/YYYY-MM-DD.md`(なければテンプレから新規作成、あれば追記)
- **何を**: その日やったこと / 決めたこと(Decisionへのリンク) / 次にやること / 引き継ぎ事項

### Decision(判断基準)
- **いつ**: 何かを選んだ・決めた・方針を変えたとき
- **どこ**: `Decision/YYYYMMDD_件名.md`(1決定1ファイル)
- **何を**: 決めたこと / **なぜそう決めたか** / 検討した他の案と却下理由 / 今後も適用するルールか単発か

### Knowledge(ナレッジ)
- **いつ**: 課題・エラーを解決したとき、手順や設定が固まったとき
- **どこ**: `Knowledge/テーマ名.md`(同テーマの既存ノートがあれば追記して育てる)
- **何を**: 何が起きたか / 原因 / 解決手順(再現できる粒度) / 参考リンク

### 記録しないもの
- 未確定の推測、途中の思いつき(確定してからDecisionに書く)
- パスワード・口座番号・APIキー等の秘匿情報(**Vaultにも書かない**)
- 単なる会話の要約(作業・決定・知見を伴わないもの)

---

## 3. ミス防止・改善ルール(Mistakes管理)

### 記録
作業中にミスをした、または**指示と違って指摘・修正を受けた**場合、
その場で `Mistakes/YYYYMMDD_ミスの要約.md` に記録する。謝罪文ではなく**再発防止の材料**として書く。

必須項目:
- **何をした**(実際の誤った行動)
- **本来どうすべきだったか**(正しい対処法)
- **なぜ間違えたか**(思い込み・確認不足・ルール未読 等)
- **再発防止チェック**: 次に同じ状況で自分に問うべき一文

### 再発防止の徹底
- 新しいタスクに入る前に `Mistakes/` を検索し、**同じ過ちを繰り返さないよう行動を変える**
- 同じミスが2回出たら、それは個別のミスではなく**ルールの欠陥**。
  `Decision/` または `docs/` のルールそのものを直す提案をする
- 過去のMistakesノートは**消さない**。解決済みなら `status: 対策済み` を追記する

---

## 4. 書き込みの安全ルール

- **追記優先**: 既存ノートは末尾追記か、指定セクションへの挿入のみ。全文置換はしない
- **削除しない**: Vault内のファイル・行の削除はAIが単独で行わない。必要なら本人に依頼する
- **上書きが必要なとき**: 旧記述を消さず、`~~取り消し線~~` か「(YYYY-MM-DD 更新: 〜)」で履歴を残す
- **秘匿情報を書かない**: パスワード・口座情報・APIキー・個人の連絡先の羅列
- **リンクで繋ぐ**: Daily → Decision/Knowledge/Mistakes は `[[ノート名]]` で相互リンクする

---

## 5. セッション終了時の締め

作業を終えるときは、報告の前に以下を実行する。

1. `Daily/YYYY-MM-DD.md` に本日分を追記(やったこと・決めたこと・次やること・引き継ぎ)
2. 発生した決定/知見/ミスを、それぞれ Decision / Knowledge / Mistakes に記録
3. ユーザーへの最終報告に**どのノートに何を書いたか**を1行で添える

明示的に記録だけを依頼された場合は `/kioku` を使う。
