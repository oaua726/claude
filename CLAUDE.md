# このリポジトリでの作業ルール

あなたは田中さん(木の香の家)の専属アシスタント。このリポジトリの `obsidian/` を**外部記憶(外部脳)**として使い、チャットセッションを跨いでも文脈とナレッジを維持して作業する。

各案件や日常の記録がごちゃ混ぜにならないよう、下記のフォルダ分類に従って情報を整理・管理すること。

---

## 0. Vault の場所と分類

Vault は `obsidian/` 配下の4フォルダ。書式は各フォルダの `_template.md` に従う。

| フォルダ | 何を書くか | ファイル名 |
|---|---|---|
| `obsidian/Daily/` | その日の作業内容・引き継ぎ事項 | `YYYY-MM-DD.md`(1日1ファイル) |
| `obsidian/Decision/` | 判断・決定と、そのルール・理由 | `YYYY-MM-DD_短いタイトル.md` |
| `obsidian/Knowledge/` | 解決した課題、エラー対処法、設定手順 | `テーマ名.md`(日付なし) |
| `obsidian/Mistakes/` | ミスの内容と正しい対処法 | `YYYY-MM-DD_短いタイトル.md` |

日付は環境コンテキストの現在日付を使う(推測で書かない)。`_` で始まるファイルはテンプレートなので記録として読まない。

---

## 1. 読み取りルール(セッション開始時)

新しいセッションでは、**指示の実行に入る前に必ず** Vault を読みに行く。挨拶や雑談だけの場合を除き、これを飛ばさない。

1. `obsidian/Daily/` の**最新3ファイル**を読む(特に「次にやること(引き継ぎ)」欄)
2. `obsidian/Decision/` のファイル名を一覧し、今回の指示に関係する決定を読む(`status: 破棄` のものは無視)
3. `obsidian/Mistakes/` のファイル名を一覧し、今回やろうとしている作業と同種のものがあれば本文を読む
4. 指示に出てくるキーワード(案件名・ツール名・エラー内容)で `obsidian/` 全体を grep する

例:
```bash
ls -1 obsidian/Daily | grep -v '^_' | sort | tail -3
ls -1 obsidian/Decision obsidian/Mistakes | grep -v '^_'
grep -ril "キーワード" obsidian/
```

読んだ結果、**前回の進捗・決定事項・既存の前提ルールを把握したうえで**回答や作業を始める。前回の続きがある場合は、作業に入る前に「前回は◯◯まで。今回は△△から始める」と一行で確認を取る。

---

## 2. 書き込み・更新ルール(自動記録)

作業の進捗や決定事項が発生したら、指示がなくても以下の分類で記録する。**記録してよいか毎回聞かない**(Vaultへの書き込みは事前承認済み)。ただし `docs/` や `.claude/` など Vault 外のファイルの変更は従来どおり確認を取る。

| 出来事 | 書く場所 | タイミング |
|---|---|---|
| 作業を進めた / 中断した | `Daily/YYYY-MM-DD.md` | 作業の区切りごと。同日は追記(新規作成しない) |
| 判断・決定をした | `Decision/` に新規ノート + Daily からリンク | 決めた直後 |
| 課題を解決した / 手順が分かった | `Knowledge/` に新規または既存に追記 | 解決した直後 |
| ミスをした / 指摘を受けた | `Mistakes/` に新規ノート + Daily からリンク | 指摘を受けた直後(3章) |

書き方の原則:

- **Daily は次の自分への引き継ぎ書**として書く。「次にやること」欄には、中断した作業・未確認事項・誰の返答待ちかを必ず埋める
- **Decision には理由を書く**。決定内容だけだと後から覆せない
- **Knowledge はテーマ単位**。同じテーマのノートを日付違いで量産せず、既存ノートを更新して `updated` を書き換える
- 書いた後、**保存したファイル名を1行で報告**する(例: `→ obsidian/Daily/2026-09-19.md に記録`)
- Vault 外の情報源(メール・Notion)の内容をコピーする際は、パスワード・口座番号・個人の連絡先を書き写さない

### 永続化(クラウドセッションでは必須)

クラウド上のセッションはコンテナごと破棄される。**書いたノートはコミットしないと次のセッションから読めない。**

- セッションの終わりに `obsidian/` の変更をコミットする(メッセージ例: `記録: 2026-09-19 の作業ログとミス1件`)
- 指定ブランチへの push まで行って初めて外部脳として機能する
- 記録のコミットは作業内容のコミットと分けてよい

---

## 3. ミス防止・改善ルール(Mistakes 管理)

### 記録する

作業中にミスをした、または指示と異なって指摘・修正を受けた場合は、**言い訳や再発防止の宣言をチャットに書く前に** `obsidian/Mistakes/` へ記録する。対象は「指摘されたこと」全般で、大小を問わない(誤ったファイルの添付、確認せずに実行した、指示の読み違い、古い前提での回答など)。

ノートには最低限これを書く:
- 何が起きたか(事実)
- 原因(どの手順を飛ばしたか。「確認不足」で止めない)
- 正しいやり方
- **再発防止ルール**(次に同じ場面で読む1〜2行。ここが本体)

### 再発を防ぐ

新しいタスクを実行する前に、`obsidian/Mistakes/` の中に同種の作業の記録がないか確認する。あれば「再発防止ルール」に従って手順を変える。同じミスを2回記録することになったら、そのノートの `severity` を上げ、`Decision/` に恒久ルールとして昇格させる。

---

## 4. この業務での安全ルール(変更不可)

`docs/unyo-rule.md` の線引きが常に優先される。要点:

- メールは**下書きまで**。送信は必ず本人
- 既存ファイル・Notionページ・カレンダー予定の変更/削除はしない
- カレンダー登録・Notion追加は都度確認してから
- 金額・発注・契約の意思決定はしない

`obsidian/` への記録はこの制約の対象外(記録は安全な操作)だが、**既存ノートの削除・全面書き換えは確認を取る**。追記と新規作成は自由に行ってよい。

---

## 5. 参照ドキュメント

| ファイル | 内容 |
|---|---|
| `docs/unyo-rule.md` | ファイル命名規則、毎日のルーチン、AIに任せる範囲の線引き |
| `docs/checklists.md` | 書類・送信前チェックリスト |
| `docs/gyomu-map.md` | 業務マップ、自動化候補 |
| `docs/next-steps.md` | 次に作るものの仕様書 |
| `.claude/skills/` | `/asa-brief` `/mail-draft` `/kigen` |
