# Claude Code 運用ルール

## セッション開始時の必須手順

新しいセッションを開始するたびに、以下のファイルを**必ず**読み込むこと：

1. `memory/profile.md` — ユーザーの設定・好み・背景
2. `memory/context.md` — 現在進行中のプロジェクト状況
3. `memory/mistakes/` — 過去のミス一覧（同じミスを繰り返さないため）

読み込んだ後、「前回の状況を把握しました」と一言報告してから作業に入ること。

## 書き込みのルール

以下の条件で `memory/` 以下のファイルを更新すること：

- **セッション終了時**: `memory/sessions/YYYY-MM-DD.md` に作業サマリーを記録
- **重要な決定をした時**: `memory/decisions/` に決定内容と理由を記録
- **ミスを犯した時**: `memory/mistakes/` に記録し、再発防止策を明記
- **ユーザーの好みが判明した時**: `memory/profile.md` を更新

## ミスの記録フォーマット

`memory/mistakes/YYYY-MM-DD-{内容}.md` として保存：

```
## ミス内容
何を間違えたか

## 状況
どのような状況で発生したか

## 再発防止策
次回からどう対処するか
```

## プロジェクト概要

このリポジトリはレトロフューチャーなネオンスタイルのステッカーを生成するPythonツール。
- メインスクリプト: `generate_stickers.py`
- 出力先: `output/`
- 依存ライブラリ: Pillow, numpy
