# Claude スキル おすすめランキング

Claude Code で使用できるスキル (`/skill-name` 形式で起動) を、汎用性・活用頻度・インパクトの観点からランキングしました。

---

## 🥇 第1位 — `review`
**PR のコードレビューを自動実施**

プルリクエストの差分を解析し、バグ・品質・設計上の問題を指摘します。レビュアーの負担を大幅に削減でき、チーム開発で最も費用対効果が高いスキルです。

- 使いどころ: PR を出す前・マージ前の最終チェック
- 呼び出し例: `/review`

---

## 🥈 第2位 — `security-review`
**セキュリティ観点の差分レビュー**

現在のブランチの変更に対してセキュリティ審査を実施します。OWASP Top 10 相当の脆弱性を検出し、本番リリース前の安全確認に必須です。

- 使いどころ: 認証・入力処理・外部 API 連携などの実装後
- 呼び出し例: `/security-review`

---

## 🥉 第3位 — `run`
**アプリを実際に起動して動作確認**

CLI・サーバー・ブラウザアプリを自動で起動し、変更が実際に機能するか確認します。テストだけでは検出できない UI や実行時の問題を発見できます。

- 使いどころ: 新機能追加後・バグ修正後の動作確認
- 呼び出し例: `/run`

---

## 第4位 — `verify`
**特定の変更が意図通りか検証**

アプリを操作しながら、特定のフロー（ゴールデンパス・エッジケース）が正しく動くかを確認します。「`run` で起動 → `verify` で検証」の流れが理想的です。

- 使いどころ: 修正した機能のピンポイント確認、PR マージ前の最終動作保証
- 呼び出し例: `/verify`

---

## 第5位 — `update-config`
**設定ファイル (`settings.json`) の自動管理**

「コマンド実行前に必ずリントする」などの自動化ルール、権限設定、環境変数を `settings.json` に反映します。Claude に記憶させるだけでは実現できない自動化をフックとして登録できます。

- 使いどころ: 権限の追加・変更、繰り返し実行したい処理の自動化
- 呼び出し例: `/update-config`

---

## 第6位 — `claude-api`
**Anthropic SDK アプリのビルド・デバッグ・最適化**

`anthropic` ライブラリを使ったアプリに特化。プロンプトキャッシュ・Tool Use・Thinking・Batch API などの高度な機能実装をサポートします。モデルのバージョン移行にも対応。

- 使いどころ: Claude API を使ったアプリ開発・チューニング・モデル移行
- 呼び出し例: `/claude-api`

---

## 第7位 — `init`
**`CLAUDE.md` の自動生成**

リポジトリを解析して、Claude が理解できるコードベースのドキュメント (`CLAUDE.md`) を生成します。新しいプロジェクトを始める際や、引き継ぎ時に一度実行するだけで効果大です。

- 使いどころ: 新規プロジェクト開始時、既存プロジェクトへの初参加時
- 呼び出し例: `/init`

---

## 第8位 — `simplify`
**変更済みコードのリファクタリング提案**

変更されたコードを対象に、再利用性・品質・効率の観点でレビューし問題を修正します。機能追加後のコード整理に役立ちます。

- 使いどころ: 実装完了後のコード品質向上
- 呼び出し例: `/simplify`

---

## 第9位 — `fewer-permission-prompts`
**権限プロンプトの削減設定**

過去のトランスクリプトを解析し、頻繁に使う読み取り専用のコマンドを自動で許可リストに追加します。作業中断を減らし、スムーズな操作感を実現します。

- 使いどころ: 同じ権限確認が繰り返し出てきてストレスを感じたとき
- 呼び出し例: `/fewer-permission-prompts`

---

## 第10位 — `session-start-hook`
**Web セッション開始時のフック設定**

Claude Code on the Web でリポジトリを使う際、セッション開始時にテストやリンターが自動で実行されるよう `SessionStart` フックをセットアップします。

- 使いどころ: Web 経由で Claude Code を使う開発環境のセットアップ
- 呼び出し例: `/session-start-hook`

---

## 第11位 — `loop`
**定期実行タスクの設定**

指定した間隔でコマンドやスキルを繰り返し実行します。デプロイ監視や定期チェックの自動化に使えます。

- 使いどころ: 「5分おきにデプロイ状況を確認」などの繰り返しタスク
- 呼び出し例: `/loop 5m /verify`

---

## 第12位 — `keybindings-help`
**キーボードショートカットのカスタマイズ**

`~/.claude/keybindings.json` を編集して、キーバインドの変更・追加・コード入力の効率化を行います。操作に慣れてきたユーザー向けの上級設定です。

- 使いどころ: ショートカットを自分好みに変えたいとき
- 呼び出し例: `/keybindings-help`

---

## まとめ表

| 順位 | スキル名 | 一言 | 汎用性 |
|------|----------|------|--------|
| 1 | `review` | PR レビューを自動化 | ★★★★★ |
| 2 | `security-review` | セキュリティ審査 | ★★★★★ |
| 3 | `run` | アプリ起動・確認 | ★★★★☆ |
| 4 | `verify` | 変更の動作保証 | ★★★★☆ |
| 5 | `update-config` | 設定・自動化管理 | ★★★★☆ |
| 6 | `claude-api` | Anthropic SDK 開発 | ★★★☆☆ |
| 7 | `init` | CLAUDE.md 生成 | ★★★☆☆ |
| 8 | `simplify` | コード整理 | ★★★☆☆ |
| 9 | `fewer-permission-prompts` | 権限プロンプト削減 | ★★★☆☆ |
| 10 | `session-start-hook` | Web セッション設定 | ★★☆☆☆ |
| 11 | `loop` | 定期実行 | ★★☆☆☆ |
| 12 | `keybindings-help` | キーバインド設定 | ★★☆☆☆ |
